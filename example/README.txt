PLEASE NOTICE:
Testing this example without a running HTTP server will not work because it uses HTTP requests.

The server must provide the content of this folder on "/".

Also, in a real application the server should also be able to serve resources such as "/data/:d" dynamically.